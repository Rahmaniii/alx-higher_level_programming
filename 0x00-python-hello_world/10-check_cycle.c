#include <stdio.h>
#include <stdlib.h>
#include "lists.h"

/**
 * check_cycle - checks cycle of node
 * @list: pointer to lists
 *
 * Return: Always 0
 */
int check_cycle(listint_t *list)
{
	listint_t *list;
	int n;

	n = 0;
	if (list == NULL)
		return (0);
	else
		return(1);;
}
